version: '3.9'

services:
  db:
    image: postgres:15
    container_name: proyecto_db
    restart: always
    env_file:
      - .env
    ports:
      - "${POSTGRES_PORT}:5432"
    volumes:
      - db_data:/var/lib/postgresql/data
      - ./db:/docker-entrypoint-initdb.d

volumes:
  db_data: